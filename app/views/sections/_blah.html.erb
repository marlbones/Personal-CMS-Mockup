<ul>
  <% @sections.each do |section| %>
    <li><%= section.title %></li>
    <%= link_to 'Show', section, remote: true %>
    <%= link_to 'Edit', edit_section_path(section) %>
    <%= link_to 'Destroy', section, method: :delete, data: { confirm: 'Are you sure?' } %>
  <% end %>
</ul>

<%= link_to 'New Section', new_section_path %>
